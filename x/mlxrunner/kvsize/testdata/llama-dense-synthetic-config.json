{
  "_comment": "Synthetic, hand-written: a dense Llama with no head_dim (so the estimator must derive hidden_size / num_attention_heads, as llama.go:112 does) and an fp32 checkpoint (so elem_bytes must be 4, not the bf16 default).",
  "architectures": ["LlamaForCausalLM"],
  "model_type": "llama",
  "torch_dtype": "float32",
  "hidden_size": 512,
  "num_hidden_layers": 4,
  "num_attention_heads": 8,
  "num_key_value_heads": 2,
  "intermediate_size": 1376,
  "rms_norm_eps": 1e-05,
  "rope_theta": 10000.0,
  "max_position_embeddings": 8192,
  "vocab_size": 32000
}
