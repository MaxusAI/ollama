# Paths check_no_names.py skips. Paths only, never names: this file is tracked.
# A path ending in / covers every file under that directory. Each entry says why.

# The invoice fixture draws its customer line into visimgs/document.png, which every
# host sends to the models in multi_3img and multi_3img_anchored. Changing the text
# regenerates the image and moves every recorded score; it waits for a planned
# benchmark re-baseline (#380 left it on purpose).
docs/maxusai/vision-suite/gen_scenes.py

# Upstream tokenizer test data: the match is a vocabulary token, not a reference to anyone.
mlxrunner/tokenizer/testdata/

# Commit messages that predate the rule, by full SHA (a SHA names no one); the commit's
# MESSAGE is skipped, nothing else. The v0.34.4 fold's merge commit (#375) was written
# before AGENTS.md's rule landed (#381). Rewording it would re-hash every later fold
# commit, including 29ae52351, the commit all three hosts' gate images are built from.
commit c3e393d565c7ca6892f1950e9926b7495ec61396
