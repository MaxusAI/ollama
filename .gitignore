.DS_Store
.vscode
.env
.venv
.swp
dist
build
.cache
.gocache
*.exe
.idea
test_data
*.crt
__debug_bin*
llama/build
llama/vendor
/ollama
integration/testdata/models/

# Ad-hoc local build outputs (e.g. ollama-dynres, ollama-test-routes). `/ollama`
# above only covers the default binary name; a `go build -o ollama-<something>` lands
# a ~50 MB artifact that `git add -A` will happily commit.
/ollama-*
